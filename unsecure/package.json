{
  "name": "monorepo-secure-demo-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  }
}